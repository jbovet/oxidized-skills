---
name: clean-ts-skill
description: Performs safe TypeScript operations via typed API calls. Use when the user needs to interact with external services using structured HTTP clients.
allowed-tools:
  - Read
  - Glob
  - Grep
---

# Clean TypeScript Skill

This skill uses safe TypeScript code with no dangerous patterns.
